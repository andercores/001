<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.001" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <!-- Habilitar transiciones. -->
        <item name="android:windowActivityTransitions">true</item>
        <!--habilitar transiciones para contenido -->
        <item name="android:windowContentTransitions">true</item>
        <!-- Entrada y salida de los elementos-->
        <item name="android:windowEnterTransition">@transition/explode_dam</item>
        <item name="android:windowExitTransition">@transition/explode_dam</item>
    </style>
    <style name="cursoTitulo">
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/tamanyoTitulo</item>
        <item name="android:padding">@dimen/espacioTitulo</item>
    </style>
    <style name="cursoTitulo.subtitulo">
        <item name="android:textSize">@dimen/tamanyoSubtitulo</item>
    </style>
</resources>